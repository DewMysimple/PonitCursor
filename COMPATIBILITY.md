# 兼容性与验证记录

验证日期：2026-09-09。环境：Windows 11 专业版 x64、.NET Framework 4.8、Microsoft Zira Desktop（en-US）、Kokoro-82M int8 ONNX。

## 实际验证

| 场景 | 结果与验证方式 |
| --- | --- |
| Obsidian 1.13.7 实时预览 | 独立测试库中，双击、拖选、Ctrl+C 均触发实际语音请求 |
| Obsidian 1.13.7 源码模式 | 双击、拖选、Ctrl+C 均通过 |
| Obsidian 1.13.7 阅读视图 | 双击、拖选、Ctrl+C 均通过 |
| Obsidian 单词过滤 | 三种模式各 7 项：hello、world、well-known、don't、多个词、跨段落、空选区；21 项通过，测试笔记 SHA-256 不变 |
| Chrome 152.0.7977.76 | 独立浏览器配置中的本地 HTML：单词读取、多个词忽略、密码框保护通过 |
| Windows RichTextBox 文本控件 | 双击、拖选、重新选词、暂停恢复、Ctrl+C 发音、剪贴板保持、密码框忽略、正常退出通过 |
| 离线语音引擎 | Windows 系统语音在出站阻止下生成有效 WAV；Kokoro 工作进程显式禁止远程模型，从便携目录加载模型和声线，`af_heart`、`bf_emma` 均经 C# 桥接生成并开始播放 |
| 自动检查 | 87 项通过：过滤、请求过期、去重、复制意图、设置损坏恢复、系统语音、28 个 Kokoro 英文声线发现、Kokoro 美英声线合成、预热后取消保活、旧推理卡死恢复、PCM 词头保留、暂停恢复、取词超时与取消等；另有 Kokoro 直接推理冒烟测试 |
| 设置窗口 | 已渲染并检查中文排版、显示状态和控件布局 |

Obsidian 完整链路测试中，从第一次鼠标按下到程序提交语音请求约为 0.69～1.06 秒，包含双击动作本身约 0.2 秒。初次建立辅助功能树较慢；不同电脑和笔记内容会影响速度。这是程序请求时延，并非声学测量的扬声器出声时延。

Obsidian 验证使用独立配置和测试库；没有修改用户现有笔记或安装插件。重新启动的独立 Obsidian 实例也进行了验证，无需强制开启辅助功能的启动参数。

## 尚未完整验证

- Edge、Firefox、Word、VS Code、管理员权限窗口、特殊主题或插件改变的文档视图。
- PDF、扫描件、图片、OCR、远程桌面内的文字。
- 多显示器混合 DPI、设备休眠恢复、音频设备热插拔、长时间运行资源曲线。
- Kokoro 在低性能 CPU 上的首轮加载和发音延迟、长时间内存占用，以及更长时间、更大规模的压力测试。
- 离线测试采用针对程序的出站网络阻止，没有切断整台电脑网络。没有实际卸载系统英文语音；语音缺失分支通过测试模拟验证。

不支持的场景可尝试 Ctrl+C 补充发音。若连控件状态都无法读取，程序会保守跳过并在设置页提示。

## 兼容方式

优先使用 UI Automation TextPattern。某些 Electron 版本的窗口层未暴露正文选区，因此进一步定位其正文渲染窗口，通过 IAccessible2 读取完整选区。只读选区与控件状态，不注入脚本、不主动复制、不修改软件配置。

首版针对鼠标拖选和双击选词；纯键盘选词可使用 Ctrl+C。分散选区或跨格式节点选区可能被跳过，以免把多个词的一部分误读。

## 2026-09-09 语音延迟与词头修复复验

- 本机 i7-14700KF：旧版工作进程模型就绪约 1.5 秒，短词推理约 0.5～0.6 秒；新版启动即预热，完整工作进程就绪约 1.0～2.4 秒，在后台完成。预热后 C# 请求到播放开始：Kokoro hello 约 0.65 秒，快速取消后最新词约 0.56～0.64 秒；Windows Zira 约 3～12 毫秒。不含选区等待、取词和音箱声学延迟。
- 显式 4 线程与默认线程池的模型阶段测试分别约 0.5 秒与 1.5 秒；这项阶段计时不等于完整程序冷启动。
- 10 次快速替换与停止后，只播放最新请求，普通取消前后 Node PID 不变。模拟卡死旧推理在取消后约 1.04～1.08 秒终止，后续可重启。
- 安静环境下对 Zira、Kokoro af_heart、bf_emma 各做首次与间隔后播放回录。整词与开头 100 毫秒相关性均超过 0.9998，未见软件输出丢失词头。回录仅代表 Windows 默认输出，物理音箱／耳机仍需用户试听。
- 两种引擎统一 PCM16、24 kHz、单声道，保留全部合成采样；启用时保持共享输出会话，设备打开时 150 毫秒静音准备。实际蓝牙、HDMI 热插拔和睡眠恢复尚未完整验证。
- 运行时清单覆盖 290 个文件，构建检查 SHA-256；程序启动检查大小，拦截未还原的 LFS 指针；诊断脚本支持完整哈希和固定词合成。
