---
type: state
status: active
kind: architecture
importance: high
updated: 2026-09-11
topic: system-architecture
source_logs:
  - "[[日志/2026-09-11-提升Zira划词稳定性与响应速度]]"
  - "[[日志/2026-09-09-修复语音延迟与词头播放]]"
  - "[[日志/2026-09-09-初始化项目与工程记忆]]"
  - "[[日志/2026-09-09-集成Kokoro离线语音]]"
supersedes: null
---

# 系统架构

> 只记录当前有效的模块关系、数据流、运行方式和关键技术边界。

## 架构概览

PointCursor 是一个无窗口驻留的 WinForms 托盘应用。主进程在用户会话中安装低级鼠标、键盘钩子和剪贴板格式监听器；钩子只投递完整选择手势和明确 Ctrl+C，不为普通输入制造取消消息。选区读取由独立的 `PointCursor.Reader.exe` 完成，以便第三方辅助功能接口卡住时可以在约 900 毫秒后终止，不阻塞托盘 UI。读取结果经过单词规则和请求代次门控后交给所选语音后端：Windows 语音由后台 `System.Speech.Synthesis` 合成为 PCM，Kokoro 由独立 Node.js 工作进程完成音素转换和 ONNX 推理；两者共用保持就绪的 WASAPI 输出通道。

## 模块关系

| 模块 | 职责 | 依赖 | 入口 |
| --- | --- | --- | --- |
| `TrayApp` / `MessageWindow` | 托盘菜单、设置窗口、剪贴板与输入消息编排 | WinForms、`Native` | `src/App.cs` |
| `InputMonitor` | 全局低级鼠标/键盘钩子，只投递拖选、双击和 Ctrl+C 完成事件 | Win32 `SetWindowsHookEx` | `src/InputMonitor.cs` |
| `SelectionClient` | 启动并复用读取辅助进程，处理取消、超时和重启 | `PointCursor.Reader.exe`、命名管道式标准输入输出 | `src/SelectionClient.cs` |
| `SelectionReader` | 校验前台窗口、控件状态并优先读取 UIA TextPattern 选区 | UI Automation | `src/SelectionReader.cs` |
| `LegacySelection` | 为 Electron/Obsidian 渲染窗口补充 MSAA/IAccessible2 选区读取 | `Accessibility.dll`、`oleacc.dll` | `src/LegacySelection.cs` |
| `WordRules` / gates | 过滤单词、拒绝过期结果，并为每次明确 Ctrl+C 建立独立发音票据 | .NET 基础类库 | `src/Core.cs` |
| `SpeechService` | 汇总 Windows 与 Kokoro 英文语音，选择后端、停止旧读音并异步发声 | `System.Speech`、`KokoroSpeechEngine` | `src/SpeechService.cs` |
| `KokoroSpeechEngine` | 管理本地工作进程、请求取消、WAV 临时文件和播放生命周期 | `third_party/kokoro-runtime`、`AudioOutput` | `src/KokoroSpeechEngine.cs` |
| `AudioOutput` / `PcmPlaybackBuffer` | 保持共享播放会话、完整 PCM 采样、设备重开准备、旧音频失效 | WASAPI、COM | `src/AudioOutput.cs` / `src/PcmAudio.cs` |
| `SapiSpeechEngine` / `RuntimeAssets` | 后台系统语音合成、运行时完整性校验 | System.Speech、SHA-256 | `src/SapiSpeechEngine.cs` / `src/RuntimeAssets.cs` |
| `SettingsForm` / `AppSettings` | 中文设置、语速音量、试听和本地 XML 持久化 | WinForms、`%LOCALAPPDATA%` | `src/SettingsForm.cs` / `src/Core.cs` |

## 数据流或请求流

1. 拖动释放或双击释放后记录前台窗口、释放坐标和剪贴板序列号；普通按键、单击、右键和滚轮不向主进程发送取消事件。
2. 主进程等待 50 毫秒让目标控件完成选区更新，然后调用读取辅助进程。辅助进程先检查窗口是否为自身、密码控件或非安全状态，再尝试 UIA `TextPattern.GetSelection`，失败时对 Electron 渲染窗口尝试 IA2。
3. 读取结果限制为单个英文词（可含内部撇号或连字符）；不合规则丢弃。只有新的完整选择手势或 Ctrl+C 才替代仍在读取的旧请求；一旦取得单词，后续普通输入和窗口状态变化不会丢弃结果或停止播放。
4. 选词成功后由所选英文语音异步播放。Windows 后端在后台合成为 PCM；Kokoro 启动时预热并复用模型，生成 24 kHz PCM16 WAV，读入后立即删除。两者共用 WASAPI 输出。新词使旧音频失效，只保留最新待处理请求；普通旧推理完成后丢弃结果，取消后仍卡住约 1 秒才终止进程。
5. Ctrl+C 仅在检测到用户真实按键、且窗口产生新的剪贴板序列号后读取短文本；不主动复制、不改写剪贴板，并再次确认控件状态。每次按键获得新票据，同一选区和同一单词可以重复播报。

## 运行与部署

双击 `dist/PointCursor/PointCursor.exe` 后，主进程和同目录读取辅助进程一起运行；启动、恢复或选择 Kokoro 时后台预热并启动 `Kokoro/node.exe worker.mjs`。设置存于当前用户 `%LOCALAPPDATA%\PointCursor\settings.xml`。构建不需要 NuGet、npm 或网络，使用 Windows 自带 .NET Framework 4.8 C# 编译器并复制已纳入源码的 Git LFS 资产。退出时卸载钩子、剪贴板监听器、辅助进程、Kokoro 工作进程、临时 WAV 和语音资源。

## 相关决策

- [[决策/ADR-004-内置Kokoro离线语音后端|ADR-004 内置 Kokoro 离线语音后端]]
- [[决策/ADR-002-选区读取策略与降级路径|ADR-002 选区读取策略与降级路径]]

播放通道启用时持续提交静音，打开／重开设备时先输出 150 毫秒静音；不会裁剪合成波形，普通选词不添加固定前置延迟。暂停释放音频设备，恢复重新准备。构建对照 `assets.tsv` 校验 290 个文件的 SHA-256；启动仅做快速长度校验，诊断可做完整校验。
